# babfs
basicly a bash fetch script
## requirements:
- bash
- a *nix system
- make (for makefile)
- cc (for makefile)
- sudo (for makefile)
## install (makefile) :
```
git clone https://github.com/lovemearowlet/babfs.git
cd babfs
sudo make install
```
